# @jakewhelan/resolvr

HTTP router, PM2 & nginx a/b test reverse-proxy companion - manage a/b test cookies and automagically configure nginx with proxy settings for PM2 instances in real-time

## TODO
- Excempt Resolvr from a/b testing rules (add a fixed route in nginx.conf - /resolvr)
- Add admin bar that shows a/b cookie is active, and which one